const userController = require("./users");
const categoryController = require("./productCategory");
const productController = require("./product");

module.exports = {
  userController,
  categoryController,
  productController,
};
